# INDEPENDENT SECURITY REVIEW — VitalGuardAI.com v4.6.5 (1-Page Summary)

> **중요 고지** — 본 문서는 USAGM, Open Technology Fund(OTF), OTF Security Lab 또는 그 감사 파트너가 의뢰·승인·발간한 공식 보고서가 아닙니다. 공개된 OTF 감사 보고서의 구조와 위험 등급 관행(Critical/High/Medium/Low/Informational)을 참고한 **독립 정적 예비 검토**입니다. 동적/하드웨어 시험은 수행하지 않았습니다.

| 항목 | 내용 |
|---|---|
| 대상 파일 | `VitalGuard_AI_complete_V46_5.html` (587,520 bytes / 11,278행 / 단일 HTML) |
| 표시 버전 | VitalGuardAI.com v4.6.5 |
| 파일 SHA-256 | `ca9b81451599726220c2b8a7f52713e585673e9815b1911a21887427710286b8` |
| 평가일 / 평가 유형 | 2026-07-02 / 독립 정적 소스·설계 검토 (V4.5.4 예비 감사 후속) |

## 발견사항 집계 (Open 기준)

| CRITICAL | HIGH | MEDIUM | LOW | INFO |
|:---:|:---:|:---:|:---:|:---:|
| **0** | **0** | **1** | **1** | **2** |

## 이번 릴리스(v4.6.5)에서 수정·정적 재검증 완료

| ID | 등급 | 상태 | 요약 |
|---|---|---|---|
| VG465-01 | LOW | **FIXED** | 펫 ID·미상 기기 ID 접미사가 `Math.random`에서 CSPRNG(`crypto.getRandomValues`) 기반 `genLocalIdSuffix()`로 교체됨. 펫 ID는 Rescue Pack·보관함 키로 유입되므로 예측 불가성 확보. |
| VG465-02 | MEDIUM | **FIXED** | 파괴적 확인 모달(`danger`)이 OK 버튼에 기본 포커스 + Enter 무조건 확정이던 동작을, Cancel 기본 포커스 + Enter는 포커스된 버튼만 실행하도록 변경. Escape·배경 클릭·모달 교체는 기존대로 항상 취소. UI 외형·비파괴 흐름 변화 없음. |
| VG465-03 | LOW | **FIXED** | `V455Crypto.u8()`가 손상된 base64 입력 시 플랫폼 원시 오류 대신 정제된 `Invalid encoding` 오류로 fail-closed. |

## 잔여(Open) 발견사항

| ID | 등급 | 요약 및 권고 |
|---|---|---|
| VG465-A | MEDIUM (수용 위험) | 범용 BLE 광고 신원은 여전히 비암호학적(복제 가능 메타데이터). v4.6에서 fail-closed 자문 모드·클론 격리·SAFE 상태 제거로 완화되었으나, 고위험 운용에는 하드웨어 challenge-response 등 인증 가능한 태그가 필요. 현재 범위는 "통제된 비운용 연구 데모"로 문서화됨. |
| VG465-B | LOW | Rescue Pack 재생 방지 원장은 브라우저 프로필 한정 단일 사용이며 전역 단일 사용 보장이 아님(코드·UI 문구에 명시됨). 전역 보장이 필요하면 수신자 간 대역외 검증 절차 유지 권고. |
| VG465-C | INFO | 하드닝 코어 자체 해시는 진단용임. 배포 채널에서 전체 파일 SHA-256 + 분리 서명(minisign/Ed25519) 게시 필요 — 코드가 아닌 릴리스 절차 과제. |
| VG465-D | INFO | 복사 전용 공유(zero-egress) 정책은 위기 상황에서 통화 지연을 만들 수 있는 의도된 트레이드오프. 현장 사용성 시험 시 재평가 권고. |

## 정적 검증 결과 (v4.6.5 빌드)

CSP 부팅 시뮬레이션 **PASS**(script[0]/script[1]/style[0] 3개 인라인 리소스 모두 선언 해시와 일치, script[1] 해시 `Umo7F3WfQUU+84T7CCmaUiCSF/wvWKRp5U324eP2yGA=` 재계산 반영) · Node 문법 검사 2/2 **PASS** · 제로 이그레스/외부 참조 스캔 활성 외부 엔드포인트 **0건** · `data-vg-on*` 액션 73건 허용목록 위반 **0건**·정의 누락 **0건** · 7개 언어 i18n 키 패리티 **PASS** · 결함 마커 **0건** · 스크립트/스타일 외 마크업 diff는 버전 문자열·CSP 해시로만 한정(UI 동일성 **PASS**). V4.5.4 예비 감사의 VG-01(BLE 신원)·VG-02(가져오기 파괴적 대체)·VG-04(평문 저장)·VG-07(쿼터 삭제)은 v4.6 시리즈의 fail-closed 자문 모드, 3상태 원자적 가져오기(REPLACE 타이핑 게이트+복구 백업 강제), AES-GCM 로컬 보관함, 비삭제형 쿼터 실패로 각각 해소 상태를 정적으로 확인함.

## 종합 판단

| 종합 등급 | **MEDIUM** — High 이상 미해결 발견사항 없음. 잔여 MEDIUM 1건은 문서화된 수용 위험(비암호학적 BLE 광고 신원)으로, 연구 데모 범위 내에서 통제됨 |
|---|---|
| 권고 배포 범위 | 통제된 데모·연구 환경. 수색·구조·재난 대응의 유일한 판단 도구 사용 금지(기존과 동일) |
| 다음 단계 | 독립 벤더의 동적 시험(BLE 실기·모바일 브라우저), 배포 채널 분리 서명, 발견사항별 재시험(retest) 기록 |
| 공개 문구 | "보안 감사 완료" 또는 "OTF 승인" 표현 금지. "OTF 공개 보고서 관행을 참고한 독립 정적 예비 평가"로만 표기 |

*작성: 2026-07-02 · 본 요약은 V4.5.4 상세 예비 감사 보고서(9개 발견사항)의 후속 1장 요약이며, 등급 정의는 해당 보고서 §5와 동일합니다.*
